import { connect } from "react-redux";
import AddDepositForm from "./Components/AddDepositForm/AddDepositForm";

function App(props) {
  return (
    <div className="App">
      <AddDepositForm />
    </div>
  );
}

export default App;
